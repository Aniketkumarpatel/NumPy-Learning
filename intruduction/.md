# NumPy

## Introduction

NumPy stands for **Numerical Python**. It is a popular Python library used for **numerical computing and data analysis**.

NumPy provides a powerful data structure called an **array**, which allows us to store and process large amounts of numerical data efficiently.

## Why Do We Use NumPy?

NumPy is used for:



- Performing mathematical calculations
- Working with arrays and matrices
- Processing numerical data
- Performing statistical operations
- Data Science and Machine Learning
- Working with Pandas and other Python libraries

## Installation

To install NumPy, use the following command:

```bash

pip install numpy
...
## .shape
### .shape ka use array ke dimensions (rows aur columns) check karne ke liye hota hai.

# .size in NumPy

## .size ka use array mein total kitne elements hain ye check karne ke liye hota hai.
'''

---

### 3. `ndim.md`

```markdown
# NumPy `.ndim`

## Introduction

`.ndim` attribute ka use NumPy array ke number of dimensions (axes) check karne ke liye hota hai.

## Syntax

```python
array.ndim
  
NumPy .dtype
Introduction

.dtype ka use NumPy array ke data type ko check karne ke liye hota hai.

Common Aggregation Functions

| Function      | Use                |
| ------------- | ------------------ |
| `np.sum()`    | Total sum          |
| `np.mean()`   | Average            |
| `np.median()` | Median             |
| `np.min()`    | Minimum value      |
| `np.max()`    | Maximum value      |
| `np.std()`    | Standard deviation |
| `np.var()`    | Variance           |
| `np.prod()`   | Product            |
.>#Fancy Indexing in NumPy

Fancy indexing means selecting specific elements from a NumPy array using a list or array of indices.